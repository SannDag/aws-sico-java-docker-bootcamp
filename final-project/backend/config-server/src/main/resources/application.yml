server:
  port: 8081

spring:
  application:
    name: config-server

cloud:
  config:
    server:
      git:
        uri: ${URI}
        searchPaths: config-data
        username: ${URI_USER}
        password: ${URI_PASSWORD}